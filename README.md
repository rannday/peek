# peek

A small cross-platform C utility that prints basic system information

# Linux Building
Install dev tools `sudo pacman -S git gcc cmake make base-devel`

Use `make clean && make`

# Windows Building
Install zig `winget install zig.zig`

Use `del peek.exe & build.bat`